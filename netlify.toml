[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--include=dev"
  SECRETS_SCAN_OMIT_KEYS = "JWT_SECRET,JWT_SCAN_SECRET,SUPABASE_SERVICE_ROLE_KEY,SUPABASE_STUDENT_PHOTOS_BUCKET,STUDENT_IMPORT_DEFAULT_PASSWORD,SMTP_PASS,NODE_ENV"